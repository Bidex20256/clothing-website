export const metadata = {
  title: "Contact",
  description: "Get in touch with Temir — questions about orders, sizing, or wholesale.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
